package config

import (
	"time"
)

var AccessToken = "Your Github personal access token goes here."

var WorkDir = "/tmp/work_dir/"

var Email = "email@example.com"

var PrivateKey = "Link to your SSH private key."

var SleepTime = 15 * time.Second
